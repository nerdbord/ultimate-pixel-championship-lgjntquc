import { wizardScreen } from '../../screens/wizard-screen/wizard-screen.js';

export const swipeLeft = (app: object, fighters: object) => {
   console.log('swipe left');

   // wizardScreen(index-1)
};
export const swipeRight = (app: object, fighters: object) => {
   console.log('swipe right');
   console.log(app);
   // console.log(index)
   // index += 1
   // console.log(index)

   // while (app.firstChild) app.firstChild.remove();

   // app.append(wizardScreen(index))
};
